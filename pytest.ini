[pytest]
# Disable anchorpy pytest plugin — it requires pytest_asyncio + pytest_xprocess
# which are not installed. We don't use anchorpy test fixtures in this project.
addopts = -p no:anchorpy --basetemp=pytest_basetemp
